import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "org.adversarialwallet.app",
  appName: "Adversarial Wallet",
  webDir: "dist",
};

export default config;
