node_modules/
dist/

# Generated WASM artifacts — rebuilt from core/ source with `npm run build:wasm`.
# Never commit compiled artifacts; the adversary audits source.
core/pkg/
core/target/
.gstack/

# Local secrets — env keys for scripts, signing keystores. Never committed.
.env
.env.*
*.keystore
*.jks
